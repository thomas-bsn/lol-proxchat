<Window x:Class="LoLProximityChat.WPF.Views.AudioWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Audio" Height="560" Width="380"
        Background="#1a1a2e" ResizeMode="CanMinimize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="#e0e0e0"/>
        <Setter Property="Background" Value="#16213e"/>
        <Setter Property="BorderBrush" Value="#0f3460"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="0,4,0,12"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="#e0e0e0"/>
                    <Setter Property="Background" Value="#16213e"/>
                    <Setter Property="Padding" Value="6,4"/>
                    <Style.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="#0f3460"/>
                            <Setter Property="Foreground" Value="#c89b3c"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#0f3460"/>
                            <Setter Property="Foreground" Value="#c89b3c"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                      Padding="{TemplateBinding Padding}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="20"/>
                                            </Grid.ColumnDefinitions>
                                            <ContentPresenter Grid.Column="0"
                                                              Margin="6,4"
                                                              VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="▾"
                                                       Foreground="#888"
                                                       VerticalAlignment="Center"
                                                       HorizontalAlignment="Center"
                                                       FontSize="10"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter Margin="6,4,20,4"
                                          VerticalAlignment="Center"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          IsHitTestVisible="False">
                            <ContentPresenter.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#e0e0e0"/>
                                    <Setter Property="FontFamily" Value="Segoe UI"/>
                                </Style>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                        <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False"
                               PopupAnimation="Slide">
                            <Border Background="#16213e"
                                    BorderBrush="#0f3460"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    MinWidth="{TemplateBinding ActualWidth}">
                                <ScrollViewer MaxHeight="200">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">

            <!-- Titre -->
            <TextBlock Text="Audio" FontSize="20" FontWeight="Bold"
                       Foreground="#c89b3c" Margin="0,0,0,16"/>

            <!-- Micro -->
            <Border Background="#16213e" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Microphone" FontSize="13" FontWeight="SemiBold"
                               Foreground="#c89b3c" Margin="0,0,0,8"/>
                    <TextBlock Text="Device" FontSize="11" Foreground="#888"/>
                    <ComboBox ItemsSource="{Binding InputDevices}"
                              SelectedItem="{Binding SelectedInput}"/>
                    <TextBlock FontSize="11" Foreground="#888">
                        <Run Text="Volume — "/>
                        <Run Text="{Binding MicVolumePercent, Mode=OneWay}"/>
                        <Run Text="%"/>
                    </TextBlock>
                    <Slider Minimum="0" Maximum="1" Value="{Binding MicVolume}"
                            TickFrequency="0.05" IsSnapToTickEnabled="True"/>
                </StackPanel>
            </Border>

            <!-- Sortie -->
            <Border Background="#16213e" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Sortie audio" FontSize="13" FontWeight="SemiBold"
                               Foreground="#c89b3c" Margin="0,0,0,8"/>
                    <TextBlock Text="Device" FontSize="11" Foreground="#888"/>
                    <ComboBox ItemsSource="{Binding OutputDevices}"
                              SelectedItem="{Binding SelectedOutput}"/>
                </StackPanel>
            </Border>

            <!-- Joueurs -->
            <TextBlock Text="Joueurs" FontSize="13" FontWeight="SemiBold"
                       Foreground="#c89b3c" Margin="0,0,0,8"/>

            <ItemsControl ItemsSource="{Binding Players}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#16213e" CornerRadius="8"
                                Padding="12,8" Margin="0,3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="8" Height="8"
                                                 VerticalAlignment="Center"
                                                 Margin="0,0,6,0">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Setter Property="Fill" Value="#555"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSpeaking}" Value="True">
                                                            <Setter Property="Fill" Value="#4caf50"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="13" FontWeight="SemiBold"
                                                   Foreground="White"/>
                                        <TextBlock Text="  👁" FontSize="11" Foreground="#555">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsInFog}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                    <TextBlock FontSize="11" Foreground="#888">
                                        <Run Text="{Binding VolumePercent, Mode=OneWay}"/>
                                        <Run Text="%"/>
                                    </TextBlock>
                                </StackPanel>

                                <Button Grid.Column="1" Content="{Binding MuteIcon}"
                                        Background="Transparent" BorderThickness="0"
                                        FontSize="16" Cursor="Hand"
                                        Click="OnMutePlayer"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</Window>