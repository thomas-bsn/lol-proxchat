<Window x:Class="LoLProximityChat.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:LoLProximityChat.WPF.ViewModels"
        Title="LoL Proximity Chat" Height="560" Width="440"
        Icon="Assets/icon.ico"
        Background="#1a1a2e" ResizeMode="CanMinimize">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <DataTemplate x:Key="PlayerTemplate">
            <Border Background="#16213e" CornerRadius="8" Padding="12,8" Margin="0,3"
                    BorderThickness="1.5">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLocalPlayer}" Value="True">
                                <Setter Property="BorderBrush" Value="#c89b3c"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="★ " Foreground="#c89b3c" FontSize="13">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsLocalPlayer}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Text="{Binding SummonerName}"
                                       FontSize="13" FontWeight="SemiBold" Foreground="White"/>
                        </StackPanel>
                        <TextBlock Text="{Binding ChampionName}"
                                   FontSize="11" Foreground="#aaaaaa" Margin="0,2,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Position}" FontSize="11" Foreground="#bbbbbb"
                                   HorizontalAlignment="Right"/>
                        <TextBlock Text="☠" FontSize="14" Foreground="#666666"
                                   HorizontalAlignment="Right">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDead}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="LoL Proximity Chat" FontSize="20" FontWeight="Bold"
                           Foreground="#c89b3c"/>

                <Button Grid.Column="1" Content="🎙 Audio"
                        Background="Transparent" Foreground="#888"
                        BorderThickness="0" FontFamily="Segoe UI" FontSize="12"
                        Cursor="Hand" Click="OnOpenAudio" Margin="0,0,8,0"/>

                <Button Grid.Column="2" Content="⚙ Calibration"
                        Background="Transparent" Foreground="#888"
                        BorderThickness="0" FontFamily="Segoe UI" FontSize="12"
                        Cursor="Hand" Click="OnOpenCalibration"/>
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">

                <!-- Indicateur game -->
                <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,8,0">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="#555555"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInGame}" Value="True">
                                    <Setter Property="Fill" Value="#4caf50"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding StatusText}" FontSize="13" Foreground="#888888"/>

                <!-- Séparateur -->
                <TextBlock Text=" · " FontSize="13" Foreground="#555"/>

                <!-- Indicateur serveur -->
                <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,4,0">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="#c62828"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsServerConnected}" Value="True">
                                    <Setter Property="Fill" Value="#4caf50"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding ServerStatusText}" FontSize="11" Foreground="#888888"/>

                <!-- Bouton mute micro -->
                <Button Content="{Binding Audio.MicMuteIcon}"
                        Background="Transparent" BorderThickness="0"
                        Foreground="White" FontSize="14" Cursor="Hand"
                        Margin="12,0,0,0"
                        Visibility="{Binding IsInGame, Converter={StaticResource BoolToVis}}"
                        Click="OnToggleMute"/>
            </StackPanel>
        </StackPanel>

        <!-- Info joueur local -->
        <Border Grid.Row="1" CornerRadius="8" Padding="12" Margin="0,0,0,12"
                Background="#0f3460"
                Visibility="{Binding IsInGame, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="{Binding PlayerList.LocalPlayerInfo}"
                       FontSize="13" FontWeight="SemiBold" Foreground="#c89b3c"/>
        </Border>

        <!-- Listes joueurs -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Team Blue" FontSize="13" FontWeight="SemiBold"
                           Foreground="#1976d2" Margin="0,0,0,4"
                           Visibility="{Binding IsInGame, Converter={StaticResource BoolToVis}}"/>
                <ItemsControl ItemsSource="{Binding PlayerList.OrderTeam}"
                              ItemTemplate="{StaticResource PlayerTemplate}"/>

                <TextBlock Text="Team Red" FontSize="13" FontWeight="SemiBold"
                           Foreground="#c62828" Margin="0,12,0,4"
                           Visibility="{Binding IsInGame, Converter={StaticResource BoolToVis}}"/>
                <ItemsControl ItemsSource="{Binding PlayerList.ChaosTeam}"
                              ItemTemplate="{StaticResource PlayerTemplate}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>